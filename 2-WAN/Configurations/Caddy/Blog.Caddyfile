# Only used to serve precompressed assets + site itself to VPS.
# Not apart of main Caddyfile
{
  auto_https off
}

:7000 {
  root * /app/www
  file_server {
    precompressed br gzip zstd
  }
  header {
    -Server
  }
}
